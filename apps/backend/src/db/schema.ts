/**
 * Kysely Database type. Populated by `kysely-codegen` (run against a live DB)
 * or hand-maintained alongside migrations. Phase 2 ticket 2.1 ships an empty
 * shape; subsequent tickets (2.3 sign-up, 2.4 verification, 2.8 profile, 2.11
 * booking-lifecycle, 2.12 jobs+applications, 2.13 messaging, etc.) introduce
 * the tables.
 */
export interface Database {}
