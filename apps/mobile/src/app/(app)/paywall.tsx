/**
 * Paywall / subscription gate (native + narrow web). The screen body lives in
 * `@/screens/parent/Paywall` so the `.web.tsx` desktop dispatcher can render the
 * same native UI at phone width.
 */
export { default } from '@/screens/parent/Paywall';
