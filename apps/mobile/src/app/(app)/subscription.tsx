/**
 * Subscription — the clinical Provider's listing & billing hub (OH-222). A hidden
 * (app) route reached from Account; the screen body lives in
 * `@/screens/provider/Subscription` (RN primitives, so it also serves phone-width
 * web without a `.web.tsx` sibling — mirrors provider-profile).
 */
export { default } from '@/screens/provider/Subscription';
