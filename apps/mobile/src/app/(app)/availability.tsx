/**
 * Consultation availability editor (native + narrow web). The screen body lives
 * in `@/screens/provider/Availability` so the `.web.tsx` desktop dispatcher can
 * render the same native UI at phone width without a circular import.
 */
export { default } from '@/screens/provider/Availability';
