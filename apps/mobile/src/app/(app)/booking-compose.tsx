/** Booking compose — Parent composes an Offer to a Caregiver (native + narrow web).
 * Body lives in `@/screens/parent/BookingCompose` so the `.web.tsx` desktop dispatcher
 * can render the same native UI at phone width without a circular import. */
export { default } from '@/screens/parent/BookingCompose';
