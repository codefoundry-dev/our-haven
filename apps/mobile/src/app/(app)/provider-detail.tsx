/**
 * Provider detail (native + narrow web). The screen body lives in
 * `@/screens/parent/ProviderDetail` so the `.web.tsx` desktop dispatcher can
 * render the same native UI at phone width.
 */
export { default } from '@/screens/parent/ProviderDetail';
