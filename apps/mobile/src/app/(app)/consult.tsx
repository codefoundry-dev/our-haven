/** Live consultation session (native + narrow web). Body lives in `@/screens/provider/Consult`
 * so the `.web.tsx` desktop dispatcher can render the same native UI at phone width without a
 * circular import. */
export { default } from '@/screens/provider/Consult';
