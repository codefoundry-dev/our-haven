/**
 * My Jobs hub (native + narrow web). The body lives in `@/screens/parent/MyJobs`
 * so the `.web.tsx` desktop dispatcher can render the same native UI at phone width.
 */
export { default } from '@/screens/parent/MyJobs';
