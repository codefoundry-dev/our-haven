/** Job apply (native + narrow web). Body lives in `@/screens/caregiver/JobApply` so the
 * `.web.tsx` desktop dispatcher can render the same native UI at phone width without a circular import. */
export { default } from '@/screens/caregiver/JobApply';
