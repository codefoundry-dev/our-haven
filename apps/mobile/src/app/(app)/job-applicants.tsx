/**
 * Job applicants (native + narrow web). The screen body lives in
 * `@/screens/parent/JobApplicants` so the `.web.tsx` desktop dispatcher can
 * render the same native UI at phone width.
 */
export { default } from '@/screens/parent/JobApplicants';
