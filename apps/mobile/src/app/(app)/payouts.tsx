/**
 * Payouts — the Caregiver's read-only payouts list (OH-221). A hidden (app)
 * route reached from the Account tab; the body lives in
 * `@/screens/caregiver/Payouts` (RN primitives, so it serves phone-width web
 * without a `.web.tsx` sibling — mirrors profile-builder / subscription).
 */
export { default } from '@/screens/caregiver/Payouts';
