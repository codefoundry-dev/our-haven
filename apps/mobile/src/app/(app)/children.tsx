/** Children roster (native + narrow web). Body lives in `@/screens/parent/Children` so the
 * `children.web.tsx` desktop dispatcher can render the same native UI at phone width without a circular import. */
export { default } from '@/screens/parent/Children';
