/**
 * Notification preferences — per-channel opt-outs (OH-221). A hidden (app) route
 * reached from the Account tab; the body lives in
 * `@/screens/caregiver/NotificationPreferences` (RN primitives, so it serves
 * phone-width web without a `.web.tsx` sibling).
 */
export { default } from '@/screens/caregiver/NotificationPreferences';
