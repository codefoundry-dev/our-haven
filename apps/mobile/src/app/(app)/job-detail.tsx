/** Job detail — a Caregiver viewing a Parent's posted Job (native + narrow web). The body lives in
 * `@/screens/caregiver/JobDetail` so the `.web.tsx` desktop dispatcher can render the same native UI
 * at phone width without a circular import. */
export { default } from '@/screens/caregiver/JobDetail';
